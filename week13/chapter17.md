# 💾 17장 타입스크립트 노드 개발 

## 1. 타입스크립트(TS) 개요 및 환경 설정

* **개념:** 자바스크립트에 명시적 타입을 추가한 언어임. 노드는 JS만 실행 가능하므로 `tsc` 컴파일러를 통해 변환 과정이 필요함.
* **초기 설정:**
1. `npm i typescript` 설치
2. `npx tsc --init`으로 `tsconfig.json` 생성


* **핵심 설정 필드:**
* `target`: 변환될 JS 버전 설정 (예: `es2016`)
* `module`: 모듈 시스템 결정 (노드는 주로 `commonjs`)
* `strict`: 엄격한 타입 검사 모드 활성화 (안정성을 위해 필수)
* `esModuleInterop`: CommonJS 모듈을 ES 모듈처럼 자연스럽게 `import` 가능하게 함.



---

## 2. 핵심 문법 및 에러 대응 전략

### 💡 타이핑(Typing)과 추론

* **정의:** 변수, 매개변수, 반환값에 타입을 지정하는 행위임.
* **추론:** 초기값이 명확하면 TS가 타입을 자동으로 판단하므로 모든 곳에 타입을 적을 필요는 없음.

### 💡 주요 타입 문법

* **유니온(`|`):** `string | number`와 같이 여러 타입을 허용함.
* **선택적 속성(`?`):** 인터페이스나 객체에서 속성 뒤에 `?`를 붙여 생략 가능한 상태로 만듦.
* **제네릭(`<T>`):** 타입을 변수화하여 가변적인 대응이 가능함. (예: `Promise<Buffer>`)
* **타입 단언(`as`):** TS가 추론하지 못하는 타입을 개발자가 강제로 지정함. (예: `process.env.NODE_ENV as 'production'`)
* **Non-null 단언(`!`):** 해당 값이 절대 `null`이나 `undefined`가 아님을 TS에 알림.

---

## 3. 외부 라이브러리 및 모델 타이핑

### 💡 DefinitelyTyped (@types)

* **TS7016 에러:** JS로 작성된 패키지(Express 등)에 타입 정의가 없을 때 발생함.
* **해결:** `npm i -D @types/패키지명`을 통해 커뮤니티에서 만든 타입 정의를 설치함.
* **확인:** `npmjs.com`에서 파란색 **TS** 아이콘은 자체 포함, 흰색 **DT** 아이콘은 별도 설치 필요를 의미함.

### 💡 시퀄라이즈(Sequelize) 모델 타이핑

* **모델 클래스:** `Model`을 상속받으며 `InferAttributes` 등을 사용해 속성을 정의함.
* **관계 메서드(Mixin):** `associate`에서 설정한 관계에 맞춰 적절한 믹스인 타입을 선언해야 메서드 자동 완성이 지원됨.
* 예: `BelongsToManyAddAssociationsMixin<Hashtag, number>`


* **외래 키:** `ForeignKey<User['id']>`와 같이 다른 모델의 키 타입을 정확히 지정함.

---

## 4. 실전 개발 가이드

* **수동 import 체제:** 자동 로딩 방식은 타입 추론이 어려우므로 모델 등을 하나씩 수동으로 불러오는 방식을 권장함.
* **ECMAScript 모듈:** 타입스크립트 환경에서는 `require` 대신 `import/export` 문법을 사용함.
* **안정성 우선:** 서버는 안정성이 생명이므로 개발 편의성보다 에러 방지에 유리한 TS 도입이 필수적임.

---
